;;; uninitialized-c.el --- Uninitialized project with one dependency

;; Version: 1.0
;; Homepage: https://example.com/
;; Package-Requires: (dependency-b)

;;; Commentary:

(provide 'uninitialized-c)

;;; uninitialized-c.el ends here
